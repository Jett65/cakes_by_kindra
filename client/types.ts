 export default class Cake {
    price: number;
    size: number;
    shape: string;

    constructor(price: number, size: number, shape: string) {
        this.price = price;
        this.size = size;
        this.shape = shape;
    }
}


